# Public origin of the NestJS backend. Do not include /api/v1.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3100

# Generate a unique high-entropy value for every environment.
# Example command: openssl rand -base64 32
AUTH_SECRET=<replace-with-a-random-secret>
